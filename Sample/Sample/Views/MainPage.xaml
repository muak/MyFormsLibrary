<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    xmlns:cr="clr-namespace:MyFormsLibrary.CustomRenderers;assembly=MyFormsLibrary"
	xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms" 
	prism:ViewModelLocator.AutowireViewModel="True" 
	x:Class="Sample.Views.MainPage" Title="MainPage">

    <ContentPage.ToolbarItems>
        <cr:ToolbarItemEx  Priority="1" Resource="ios-star.svg" Command="{Binding TestCommand}"/>
        <cr:ToolbarItemEx  Priority="2" Resource="ios-star-outline.svg" Command="{Binding Test2Command}" IsVisible="{Binding ToolVisible.Value}" />
    </ContentPage.ToolbarItems>
    
	<StackLayout HorizontalOptions="Center" VerticalOptions="Center">
		<Label Text="{Binding Title}" />
	</StackLayout>
</ContentPage>
